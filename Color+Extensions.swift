import SwiftUI

extension Color {
    /// A조 휴식 — 하늘 파랑 #3399DC
    static let teamA = Color(red: 0x33 / 255, green: 0x99 / 255, blue: 0xDC / 255)

    /// 서비스 — 앰버 오렌지 #FF9400
    static let serviceOrange = Color(red: 0xFF / 255, green: 0x94 / 255, blue: 0x00 / 255)

    /// B조 휴식 — 퍼플 #AD52DE
    static let teamB = Color(red: 0xAD / 255, green: 0x52 / 255, blue: 0xDE / 255)
}
